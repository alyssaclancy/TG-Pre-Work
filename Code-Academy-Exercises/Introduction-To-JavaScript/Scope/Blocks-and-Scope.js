const city = 'New York City';
function logCitySkyline() {
  let skyscraper = 'Empire State Building';
  return 'The stars over the ' + skyscraper + ' in ' + city;
}
console.log(logCitySkyline());

/*Blocks help us group one or more statements together to form important structural markers in our code.
Above, the function can access both variables (defined within and outside of the function).
*/
