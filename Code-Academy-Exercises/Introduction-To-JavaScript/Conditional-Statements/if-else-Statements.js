let sale = true;

sale = false;

if(sale) {
  console.log('Time to buy!');
} else {
  console.log('Time to wait for a sale.')
}
/*If...else statements add an option for code to execute if the condition is false.
These allow us to automate binary decisions.
*/
