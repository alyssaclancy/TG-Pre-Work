console.log('Teaching the world how to code'.length);

/*Every string has a length property. You can retrieve it by using the dot operator and adding the word length.
The dot operator was used to get the length of the string above, which is 30 characters.
*/
