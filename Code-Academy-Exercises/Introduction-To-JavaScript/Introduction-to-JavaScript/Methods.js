// Use .toUpperCase() to log 'Codecademy' in all uppercase letters
console.log('Codecademy'.toUpperCase());

// Use a string method to log the following string without whitespace at the beginning and end of it.
console.log('    Remove whitespace   '.trim());

/*Methods are called by appending an instance with a period (dot operator), the name of the method, and opening/closing parentheses.
.log() is a method on the console object, for exmaple.
.toUpperCase() was used to make the string uppercase text and .trim() was used to remove extra spaces from beginning and end of the string.
*/
