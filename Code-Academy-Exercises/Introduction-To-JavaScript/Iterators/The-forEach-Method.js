const fruits = ['mango', 'papaya', 'pineapple', 'apple'];

// Iterate over fruits below
fruits.forEach(fruitItem =>
  console.log('I want to eat a ' + fruitItem));

/*The .forEach() method exectures the same code for each element of an array.
.forEach was used to work through the array and apply the same console.log method to each element.
*/
