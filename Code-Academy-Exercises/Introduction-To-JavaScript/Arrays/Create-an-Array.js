const hobbies = ['yoga', 'coding', 'painting'];
console.log(hobbies);

/*An array literal creates an array by wrapping items in square brackets []. Each item in an arrya is an element. An array can contain different data types
and can be saved to a variable.
The array above contains 3 strings and prints as an array.
*/
