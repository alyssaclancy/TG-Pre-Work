const plantNeedsWater = day => day === 'Wednesday' ? true : false;

/*Concise body encompasses the following:
Functions accepting a single parameter don't need parentheses around the parameter.
Function body composed of a single-line block does not need curly braces and uses an implicit return eliminating the keyword.
The function above was changed to a concise body. It has a single input and one line of code.
*/
