function makeShoppingList(item1 = 'milk', item2 = 'bread', item3 = 'eggs'){
  console.log(`Remember to buy ${item1}`);
  console.log(`Remember to buy ${item2}`);
  console.log(`Remember to buy ${item3}`);
}

/*Default parameters allow parameters to have a predefined value in case there is no argument passed when the function is called.
This is done in the parentheses in the function declaration opening line.
Default parameters were assigned to each item above.*/
