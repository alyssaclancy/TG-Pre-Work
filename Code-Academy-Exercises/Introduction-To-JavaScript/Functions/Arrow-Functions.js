const plantNeedsWater = (day) => {
  if (day === 'Wednesday') {
    return true;
  } else {
    return false;
  }
};

/*Arrow functions are a shorter way to write functions, removing the need to write function as a keyword.
Instead, you use fat arrow notation () =>
The function syntax above was updated to arrow function syntax.
*/
