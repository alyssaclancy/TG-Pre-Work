const vacationSpots = ['Bali', 'Paris', 'Tulum'];

// Write your code below
for (let i = 0; i < vacationSpots.length; i++) {
  console.log('I would love to visit ' + vacationSpots[i]);
}

/*You can loop through arrays and perform the same operation on each element in an array.
Starting with index position of 0 and moving through in incrememnts of 1 while the index is less than the total length of the array
allows you to move through each element. Above, each element is printed in a statement. 
*/
