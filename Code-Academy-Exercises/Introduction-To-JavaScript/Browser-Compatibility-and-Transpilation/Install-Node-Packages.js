var pasta = "Spaghetti"; // ES5 syntax

const meat = "Pancetta"; // ES6 syntax

let sauce = "Eggs and cheese"; // ES6 syntax

// Template literals, like the one below, were introduced in ES6
const carbonara = `You can make carbonara with ${pasta}, ${meat}, and a sauce made with ${sauce}.`;

/*Install command can be used to install new Node packagees locally. It creates a folder called node_modules,
We npm install two packages babel-cli (babel command line tools) and babel-preset-env (maps features ES6 to ES5).
-D flag instructs npm to add each package to a property called devDependencies.
$ npm install babel-cli -D
$ npm install babel-preset-env -D
then ls adds node_modules
*/
